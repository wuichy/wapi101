# Variables de entorno (NUNCA subir a git)
.env
.env.local
.env.*.local

# Dependencias
node_modules/

# Datos locales (estado, tokens, chats)
data/
*.log

# macOS
.DS_Store

# Editor
.vscode/
.idea/
*.swp

# ZIPs del widget
*.zip

# Binario de ngrok (no se necesita en deploy)
ngrok

# Build / cache
dist/
.cache/

# Claude (worktrees + local settings)
.claude/worktrees/
.claude/settings.local.json

# Backups locales (no van a git, viven en iCloud o local-only)
_safety-backups/

# Binarios y archivos locales
cloudflared
*.pdf
.env.bak

# Carpeta de pruebas locales
claude-pro-reelance/
wapi.db

# Respaldos que se hacen a mano en el servidor al editar en caliente.
# (16-sep-2026) Uno era app/.env.bak-<fecha> CON SECRETOS, y el patrón viejo
# (.env.bak exacto) NO lo cubría: un `git add -A` lo habría subido a GitHub.
.env.bak*
*.bak*
*.claudebak*
*_tmp.js
